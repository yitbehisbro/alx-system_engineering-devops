#!/usr/bin/env bash
#Transfer file from client to servers

PATH_TO_FILE=$1
IP=$2
USERNAME=$3
PATH_TO_SSH_KEY=$4

if [ "$#" -eq 4 ]
then
	scp "$PATH_TO_FILE" "$USERNAME"@"$IP":"$PATH_TO_SSH_KEY"
else
	echo "Usage: 0-transfer_file PATH_TO_FILE IP USERNAME PATH_TO_SSH_KEY"
	exit 1
fi
